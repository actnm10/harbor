<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main_root"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical" android:background="@color/harbor_background"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:layout_width="match_parent" android:layout_height="wrap_content" android:minHeight="72dp"
        android:orientation="horizontal" android:gravity="center_vertical"
        android:paddingStart="16dp" android:paddingEnd="8dp" android:paddingTop="12dp" android:paddingBottom="12dp"
        android:background="@color/harbor_surface">
        <ImageView
            android:layout_width="40dp" android:layout_height="40dp"
            android:padding="5dp" android:background="@drawable/bg_toolbar_logo"
            android:src="@drawable/ic_anchor" android:contentDescription="@null"
            android:importantForAccessibility="no" />
        <LinearLayout
            android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1"
            android:orientation="vertical" android:layout_marginStart="12dp" android:layout_marginEnd="8dp">
            <TextView
                android:layout_width="match_parent" android:layout_height="wrap_content"
                android:text="@string/app_name" android:textColor="@color/harbor_ink"
                android:textSize="18sp" android:fontFamily="sans-serif-medium"
                android:includeFontPadding="false" />
            <TextView android:id="@+id/server_label"
                android:layout_width="match_parent" android:layout_height="wrap_content"
                android:layout_marginTop="4dp" android:text="@string/personal_cloud"
                android:textColor="@color/harbor_muted" android:textSize="12sp"
                android:maxLines="1" android:ellipsize="middle"
                android:accessibilityLiveRegion="polite" android:includeFontPadding="false" />
        </LinearLayout>
        <ImageButton android:id="@+id/menu_button"
            android:layout_width="48dp" android:layout_height="48dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:padding="12dp" android:src="@drawable/ic_more"
            android:contentDescription="@string/server_options" />
    </LinearLayout>

    <ProgressBar android:id="@+id/loading_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent" android:layout_height="3dp"
        android:visibility="invisible" android:max="100" android:progress="0"
        android:progressTint="@color/harbor_accent" android:progressBackgroundTint="@color/harbor_line"
        android:contentDescription="@string/loading_server" />

    <FrameLayout android:id="@+id/content_frame"
        android:layout_width="match_parent" android:layout_height="0dp" android:layout_weight="1">
        <WebView android:id="@+id/web_view"
            android:layout_width="match_parent" android:layout_height="match_parent"
            android:background="@color/harbor_background" />
        <ScrollView android:id="@+id/error_panel"
            android:layout_width="match_parent" android:layout_height="match_parent"
            android:fillViewport="true" android:visibility="gone"
            android:background="@color/harbor_background">
            <LinearLayout
                android:layout_width="match_parent" android:layout_height="wrap_content"
                android:orientation="vertical" android:gravity="center"
                android:paddingStart="28dp" android:paddingEnd="28dp"
                android:paddingTop="40dp" android:paddingBottom="40dp">
                <ImageView
                    android:layout_width="88dp" android:layout_height="88dp"
                    android:padding="23dp" android:background="@drawable/bg_connection_icon"
                    android:src="@drawable/ic_connection" android:contentDescription="@null"
                    android:importantForAccessibility="no" />
                <TextView
                    android:layout_width="match_parent" android:layout_height="wrap_content"
                    android:layout_marginTop="28dp" android:gravity="center"
                    android:text="@string/connection_error_title" android:textColor="@color/harbor_ink"
                    android:textSize="25sp" android:fontFamily="sans-serif-medium" />
                <TextView android:id="@+id/error_message"
                    android:layout_width="match_parent" android:layout_height="wrap_content"
                    android:layout_marginTop="12dp" android:gravity="center"
                    android:text="@string/connection_error" android:textColor="@color/harbor_muted"
                    android:textSize="15sp" android:lineSpacingExtra="5dp"
                    android:accessibilityLiveRegion="polite" />
                <Button android:id="@+id/retry_button" style="@style/HarborButton"
                    android:layout_width="match_parent" android:layout_height="wrap_content"
                    android:layout_marginTop="30dp" android:text="@string/retry" />
                <Button android:id="@+id/change_server_button" style="@style/HarborButton.Secondary"
                    android:layout_width="match_parent" android:layout_height="wrap_content"
                    android:layout_marginTop="12dp" android:text="@string/change_server" />
            </LinearLayout>
        </ScrollView>
    </FrameLayout>
</LinearLayout>
